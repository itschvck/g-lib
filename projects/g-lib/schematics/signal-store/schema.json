{
  "$schema": "http://json-schema.org/schema",
  "$id": "signal-store",
  "title": "g-lib Signal Store Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the entity",
      "type": "string",
      "x-prompt": "What is the name of the entity?"
    },
    "withCredentials": {
      "description": "Add withCredentials to the service's http calls?",
      "type": "boolean",
      "x-prompt": "Add withCredentials to the service's http calls?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path at which to create the files, relative to the workspace root.",
      "default": "src/app/core/domain/entities/"
    }
  },
  "required": ["name"]
}
